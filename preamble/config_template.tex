% ALGO TUT CONFIG FILE

% PLEASE COPY THIS TEMPLATE FILE INTO A NEW `config.tex` FILE WHICH YOU CAN THEN CUSTOMIZE.

% Fill in these for customization.
\newcommand{\myname}{Max Mustermann}
\newcommand{\mynamebold}{\textbf{Max} Mustermann}
\newcommand{\mymail}{max.mustermann@student.kit.edu}  % Consider using your named student e-mail address to keep your u**** account private.
\newcommand{\mytutnumber}{42}
\newcommand{\mysocrativeroom}{???}

% Toggle ONE (!) of these
%\def\ThassesTut{}
\def\DanielsTut{}

\newcommand{\aboutMeFrame}{
	\begin{frame}{tbd.}
		\centering
		% A photo of you, if you want
		\includegraphics[height=.3\frameheight]{../customdata/some-picture-of-me.jpg} \\
		\myname \\
		\mailto{\mymail} \\ \vspace{.2\baselineskip}
		Informatik, X. Fachsemester (Bachelor). \\
		\bigskip
		% something more about you... (optional)
	\end{frame}
}

% Please insert the current year here
\def\thisyear{2019}
% PLEASE REMEMBER to change the \ILIAS command's URL inside tutpreamble.tex!

% This list is for your own tutorial dates -- please adjust accordingly
\def\mydate#1{%
	\ifnum#1=1\relax	  	  27. April \thisyear \
	\else \ifnum#1=2\relax	  04. Mai \thisyear \
	\else \ifnum#1=3\relax    11. Mai \thisyear \
	\else \ifnum#1=4\relax    18. Mai \thisyear \
	\else \ifnum#1=5\relax    25. Mai \thisyear \
	\else \ifnum#1=6\relax    01. Juni \thisyear \
	\else \ifnum#1=7\relax    08. Juni \thisyear \
	\else \ifnum#1=8\relax    15. Juni \thisyear \
	\else \ifnum#1=9\relax    22. Juni \thisyear \
	\else \ifnum#1=10\relax   29. Juni \thisyear \
	\else \ifnum#1=11\relax   06. Juli \thisyear \
	\else \ifnum#1=12\relax   13. Juli \thisyear \
	\else \ifnum#1=13\relax   20. Juli \thisyear \
	\else \textbf{Datum undefiniert!} 
	\fi\fi\fi\fi\fi\fi\fi\fi\fi\fi\fi\fi\fi
}		


\def\mylasttimestext{Was letztes Mal geschah...}

\colorlet{beamerlightred}{red!25}
\colorlet{beamerlightgreen}{green!35}
\colorlet{beamerlightyellow}{yellow!50}
\colorlet{lightred}{red!30}
\colorlet{lightgreen}{green!40}
\colorlet{lightyellow}{yellow!50}
\colorlet{fullred}{red!60}
\colorlet{fullgreen}{green}

\definecolor{myalertcolor}{rgb}{1,0.33,0.24}
\setbeamercolor{alerted text}{fg=myalertcolor}